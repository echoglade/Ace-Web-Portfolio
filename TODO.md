# TODO

- [x] Update `src/components/Contact.jsx` to remove `href="#"` from contact info links (use `mailto:` for email; safe non-navigating element for non-email items).
- [x] Expand `src/components/Contact.emailjs.test.js` with a failure/rejection test for the exported `sendEmail` helper.

- [x] Run `npm test` to ensure Vitest passes.
- [ ] Manually verify in the browser: clicking contact info does not cause unreachable/odd navigation; email sending logic unchanged.


